#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcmp - Compares two strings.
 * @s1: First string.
 * @s2: The second string.
 *
 * Return: 0 for equal strings, negative if s1 < s2, positive if s1 > s2.
 *
 */

int _strcmp(char *s1, char *s2)
{
	return (s1, s2);
}
