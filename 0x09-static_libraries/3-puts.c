#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _puts - Prints a string followed by a newline.
 *
 * @str: The string to be printed.
 *
 * Return: None (void).
 */
void _puts(char *str)
{
	puts(str);
}
